func fin<int a, int b> -> int {
	return pax(5*a, b).
}

func pax<int a, int b> -> int {
	if(a < b) {
		return b.
	}
	return pax(a, 2 * b).
}

exec {
	const a := 11.
	const b := 1.
	const c := 16.

	if(a > 5) {
		const m := pax(a,b).
		const pax := pax(a,b).
	}

	const nice := pax(a,b).
	print nice.
}